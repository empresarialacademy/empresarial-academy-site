import { PayloadLoginView } from "./PayloadLoginView";

/** Instância de PayloadLoginView configurada para este sistema (EA HUB). */
export const SiteLoginView = PayloadLoginView({ systemName: "Hub", tagline: "Central de sistemas da Empresarial Academy" });
